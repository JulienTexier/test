<%= form_with(model: join_items_order, local: true) do |form| %>
  <% if join_items_order.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(join_items_order.errors.count, "error") %> prohibited this join_items_order from being saved:</h2>

      <ul>
      <% join_items_order.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
